# Copy this file to .env and fill in your key.
# AutoScribe loads OPENAI_API_KEY from (in order): the process environment,
# the app bundle (.env copied at build time), the current directory, and
# ~/Documents/AutoScribe/.env
OPENAI_API_KEY=sk-your-openai-api-key-here
